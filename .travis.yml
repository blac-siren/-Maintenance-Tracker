language:
 - python

python:
  - 3.6

install:
  - cd api
  - pip3 install -r requirements.txt
  - pip install coveralls

script:
  - cd api
  - nosetests --with-coverage --cover-package=app
  - coveralls

after_success:
  - coveralls

after_success:
  - coveralls